#include "../headers/test_way.h"

int main()
{
    // Non-Dinamic fibonacci
    int n = -2;
    printf("%d\n", fib(n));
    // Dinamic fibonacci
    //printf("%d\n", dinamic_fib(n));
    return 0;
}   